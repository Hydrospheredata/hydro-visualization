{
  "definitions": {},
  "$schema": "",
  "$id": "http://example.com/root.json",
  "type": "object",
  "title": "",
  "default": null,
  "required": [
    "model_name",
    "model_version"
  ],
  "properties": {
    "model_name": {
      "$id": "#/properties/model_name",
      "type": "string",
      "title": "The Model_name Schema",
      "examples": [
        "adult_scalar"
      ],
      "minLength": 1,
      "pattern": "^(.*)$"
    },
    "model_version": {
      "$id": "#/properties/model_version",
      "type": "integer",
      "title": "The Model_version Schema",
      "examples": [
        1
      ]
    },
    "visualization_metrics": {
      "$id": "#/properties/visualization_metrics",
      "type": "array",
      "title": "The Visualization_metrics Schema",
      "default": null,
      "items": {
        "$id": "#/properties/visualization_metrics/items",
        "type": "string",
        "title": "The Items Schema",
        "default": "[]",
        "examples": [
          "global_score",
          "sammon_error",
          "auc_score",
          "stability_score",
          "msid",
          "clustering"
        ],
        "pattern": "^(.*)$"
      }
    }
  }
}